### 需求2：会员足迹删除功能
后端API实现：

- 在 `AdminFootprintController.java` 中新增了：
  - 单个删除接口： DELETE /admin/footprint/delete
  - 批量删除接口： POST /admin/footprint/batch-delete
- 在 `LitemallFootprintService.java` 中新增了 deleteByExample 方法
前端界面实现：

- 在 `footprint.vue` 中：
  - 添加了批量删除按钮和每行的删除按钮
  - 实现了表格行选择功能
  - 新增了删除确认对话框
  - 添加了删除成功后的列表刷新
- 在 `user.js` 中新增了删除相关的API接口函数
### 需求3：品牌制造商介绍列格式修改
前端界面优化：

- 在 `brand.vue` 中：
  - 实现了介绍列的展开/收起功能（超过100字符时显示展开按钮）
  - 添加了美观的CSS样式，使用flex布局
  - 为每行数据添加了 showFullDesc 状态属性
### 编译测试结果
- 后端代码编译成功，所有模块构建通过
- 前端代码构建成功，生成了完整的dist目录
- 两个功能都已实现并可正常使用
所有功能均已按照需求文档的要求实现完毕，您可以通过访问相应的管理页面来测试这些新功能。